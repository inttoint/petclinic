import React from 'react';
import { SLoader } from './loader.styled';

const Loader = () => {
  return (
    <SLoader>
      <span />
      <span />
      <span />
    </SLoader>
  );
};

export default Loader;
