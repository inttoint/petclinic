import styled from 'styled-components';

const UserControlsWrapper = styled.div`
  position: relative;
  border-top: 1px solid ${({ theme }) => theme.colors.secondaryMoreDarkness};
  margin-right: 0;
  margin-left: 0;
`;

export { UserControlsWrapper };
