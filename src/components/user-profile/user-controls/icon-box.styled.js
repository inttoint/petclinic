import styled from 'styled-components';

const SUserControlsWrapper = styled.div`
  position: relative;
  border-top: 1px solid ${({ theme }) => theme.colors.secondaryMoreDarkness};
  margin-right: 0;
  margin-left: 0;
`;

export { SUserControlsWrapper };
